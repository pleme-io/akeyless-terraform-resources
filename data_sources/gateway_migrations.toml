[data_source]
name = "akeyless_gateway_migrations"
description = "List gateway migrations"

[read]
endpoint = "/gateway-list-migration"
schema = "GatewayListMigration"

[fields]
token = { skip = true }
